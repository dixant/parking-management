import React from 'react';

class Initialize extends React.PureComponent {
    render() {
        return (
            <div>Initialize new parking lot here...</div>
        )
    }
}
export default Initialize;